# SDK examples

The examples cover session creation, models, prompts, global skills, tools, extensions, prompt templates, authentication, global settings, and full dependency injection.

SDK sessions always run from `$HOME`. Use `SessionManager.inMemory()` when an example must not modify the global conversation.
