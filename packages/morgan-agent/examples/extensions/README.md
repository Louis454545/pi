# Extension examples

These modules demonstrate the current global extension API. Copy an example to `~/.morgan/agent/extensions/` and run `/reload`, or pass it explicitly with the extension CLI flag.

Extensions execute with the user account’s permissions. The examples target the current global runtime and linear conversation model.
